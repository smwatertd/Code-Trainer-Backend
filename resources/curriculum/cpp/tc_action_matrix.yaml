version: 1

tier_defaults:
  beginner:
    required_actions: [translate, implement, assemble]
    optional_actions: [recognize, analyze, debug]
    disabled_actions: []
  intermediate:
    required_actions: [translate, implement, assemble]
    optional_actions: [recognize, analyze, debug]
    disabled_actions: []
  advanced:
    required_actions: [translate, implement, assemble]
    optional_actions: [recognize, analyze, debug]
    disabled_actions: []

action_masks: {}
